# Screenshots & demo artifacts

> TODO: capture after the first live demo run.

Planned captures:

1. **Pipeline trace** — the `paloma.*` log timeline of a full run
   (Architect → Developer → Validator → Report).
2. **Generated report** — `reports/R-001-<timestamp>.md` rendered.
3. **Validation firewall in action** — a deliberately tampered offer being
   rejected by `PRICE_CONSISTENCY` / `ROI_BOUNDS` rules.
4. **Test suite** — `pytest` output proving the deterministic core runs
   without any LLM key.
